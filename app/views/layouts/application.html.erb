<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Project 1 - School Management" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header style="background:#004d99; color:white; padding:1rem;">
      <nav>
        <%= link_to "Home", root_path, style: "color:white; margin-right:10px;" %> |
        <%= link_to "Schools", schools_path, style: "color:white; margin-right:10px;" %> |
        <%= link_to "Teachers", teachers_path, style: "color:white; margin-right:10px;" %> |
        <%= link_to "Students", students_path, style: "color:white; margin-right:10px;" %> |
        <%= link_to "About", about_path, style: "color:white;" if Rails.application.routes.url_helpers.method_defined?(:about_path) %>
      </nav>
    </header>

    <main style="max-width:900px; margin:2rem auto; padding:1rem;">
      <%= yield %>
    </main>

    <footer style="text-align:center; color:#666; padding:1rem; border-top:1px solid #ddd;">
      &copy; <%= Time.now.year %> School Management Project
    </footer>
  </body>
</html>
